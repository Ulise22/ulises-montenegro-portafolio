import styles from './projectPopup.module.css'

export default function ProjectPopup (changeValue) {
    return (
        <div>
            <h1>HOlaa</h1>
            <button onClick={changeValue}>xd</button>
        </div>
    )
}